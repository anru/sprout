{
  "name": "sprout",
  "version": "0.0.3",
  "description": "Change JS structures efficiently without mutating them",
  "main": "src/index.js",
  "repository": "https://github.com/herrstucki/sprout.git",
  "keywords": [
    "immutable"
  ],
  "scripts": {
    "test": "vows --spec",
    "build": "browserify -s sprout -o sprout.js -- src/index.js && uglifyjs -o sprout.min.js -- sprout.js"
  },
  "author": "Jeremy Stucki",
  "license": "BSD",
  "devDependencies": {
    "browserify": "^3.43.0",
    "uglify-js": "^2.4.13",
    "vows": "~0.7.0"
  }
}
