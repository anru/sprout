{
  "name": "sprout-data",
  "version": "0.2.1",
  "description": "A set of functions to work with nested data.",
  "main": "index.js",
  "repository": "https://github.com/herrstucki/sprout.git",
  "keywords": [
    "immutable",
    "nested",
    "object",
    "array"
  ],
  "scripts": {
    "test": "vows --spec",
    "build": "browserify -s sprout -o sprout.js -- index.js && uglifyjs -o sprout.min.js -- sprout.js"
  },
  "author": "Jeremy Stucki",
  "license": "BSD",
  "devDependencies": {
    "browserify": "^3.43.0",
    "uglify-js": "^2.4.13",
    "vows": "~0.7.0"
  }
}
